
seq [起始] [步长] [结束]

起始默认：1；步长默认：1

### 示例

seq 5            # 1 2 3 4 5
seq 2 5          # 2 3 4 5
seq 1 2 10       # 1 3 5 7 9  步长2
seq 10 -2 1      # 10 8 6 4 2  递减



sed -n 5p filename(filename省略就会读取stdin的内容)
`-n`
sed 默认会把**每一行处理完之后打印输出**；`-n` 关闭默认打印，**只输出显式用 `p`（print）命令指定的行**。
2. `5p`

- `5`：地址，代表第 5 行
- `p`：print，打印匹配到的这一行
